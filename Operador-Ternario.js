//Operador Ternario -->   teste/condição ? true : false

const pontuacao = 500;

//se a condição for true exibi usuario vip se for false exibi usuario normal
                // teste/condição   //se for true   //se for false
const usuario = pontuacao >= 1000 ? 'usuario vip' : 'usuario normal';

console.log(usuario);

//se a condição for true exibi usuario vip se for false exibi usuario normal