const nome1 = 'everton'; //string
const nome2 = "everton"; //strin
const nome3 = `everton`; //string
const num1 = 10; // number
const num2 = 10.50; // number. chamado de numero de ponto flutuante, por causa da virgula
let NomeAluno1; // undefine -> nao aponta pra local nenhum na memoria, porque apenas declaramos a variavel
let SobreNomeAluno1 = null; //nulo > nao aponta pra local nenhum na memoria
const aprovado = true; //valor booleano (bolean)
const reproado = false; //valor booleano (boolean) 

/* ================================================================================================================================================================= */

/* podemos usa o comando typeof para descobrir o tipo da variavel e o valor correspondente a ela exemplo abaxio: */

                    //tipo da variavel           //valor que esta dentro da variavel
console.log(typeof      nome1,                              nome1)
